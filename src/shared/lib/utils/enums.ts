export enum MENU_ITEMS {
  Category = "category",
  Stores = "shops",
  Products = "products",
  Properties = "property",
}

export enum PropertyRenderType {
  Image = 14200,
  Text = 14201,
}

export enum Mode {
  create = "create",
  update = "update",
}
