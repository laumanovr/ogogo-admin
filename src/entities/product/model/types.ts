export interface ProductEntity {
  id: string;
  productName: string;
  productType: number;
  status: number;
  iconBase64: string;
  verifierDate: string;
  verifierId: number;
  verifierName: string;
  shopId: string;
  shopName: string;
  shopIconBase64: string;
  changes: string;
}
